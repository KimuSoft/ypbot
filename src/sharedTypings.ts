export type YPUser = {
    id: string
    tag: string
    avatar: string
}

export type YPGuild = {
    id: string
}

export type Guild = {
    id: string
    icon: string
    invited: boolean
    name: string
}

export type YPChannel = {
    id: string
    name: string
    desc: string
}
